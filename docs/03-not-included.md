# What's not included

Omarchy assumes an x86_64 laptop. This is everything from its package lists that
does not come along to a CM5 uConsole, and why.

This file is generated by `bin/gen-not-included.sh` from `packages/triage.tsv`
and `packages/excluded.packages`. Don't edit it by hand.

## Summary

| Verdict | Count |
|---|---|
| Already in Arch Linux ARM aarch64 | 136 |
| Dropped (see below) | 44 |
| Built here from omarchy-pkgs | 20 |
| Built here from the AUR | 7 |

## Dropped features

Three things are missing that are not merely a package:

**Snapshot rollback from the boot menu.** Omarchy pairs btrfs snapshots with
limine boot entries so a bad update can be backed out at boot. The CM5 has no
BIOS or UEFI — its bootloader is in SPI EEPROM and reads `config.txt` — so there
is no boot menu to add entries to. We ship ext4 and no snapper. Rolling back an
update means reflashing, or restoring from your own backups.

**Hardware video encode/decode.** Omarchy's screen recording and video tooling
lean on VAAPI via Intel or NVIDIA drivers. The CM5 has neither. The VideoCore
VII has its own decode path, but nothing here is wired up to use it, so
`obs-studio` and `gpu-screen-recorder` will fall back to software encoding if
they run at all — on four A76 cores, expect that to be slow.

**The 4G modem.** Some uConsoles have one. Rex ships a Debian `uconsole-4g`
package; no Arch equivalent has been written and ModemManager is not installed.

## Dropped packages

Every package below is in Omarchy's own lists and deliberately excluded.

| Package | Reason |
|---|---|
| `apple-bcm-firmware` | Apple T2 MacBook firmware |
| `apple-t2-audio-config` | Apple T2 MacBook audio |
| `asdcontrol` | Apple Studio Display brightness control |
| `asusctl` | ASUS laptop firmware control |
| `bolt` | Thunderbolt device manager; the CM5 has no Thunderbolt |
| `broadcom-wl` | x86 DKMS Broadcom driver; the CM5 uses the in-kernel brcmfmac driver |
| `btrfs-progs` | not used; root filesystem is ext4 so it can be grown in place on first boot |
| `ddcutil` | DDC/CI control for external monitors over I2C; the internal DSI panel has no DDC |
| `dell-xps-touchpad-haptics` | Dell XPS hardware |
| `intel-ipu7-camera` | Intel IPU7 camera stack; x86-only |
| `intel-lpmd` | Intel low-power daemon; x86-only |
| `intel-media-driver` | Intel GPU video acceleration; x86-only |
| `lib32-nvidia-580xx-utils` | no NVIDIA GPU, and no 32-bit multilib on aarch64 |
| `lib32-nvidia-utils` | no NVIDIA GPU, and no 32-bit multilib on aarch64 |
| `libva-intel-driver` | Intel GPU video acceleration; x86-only |
| `libva-nvidia-driver` | no NVIDIA GPU on a CM5 |
| `libvpl` | Intel oneVPL video encode/decode; x86-only |
| `limine-mkinitcpio-hook` | depends on limine |
| `limine-snapper-sync` | depends on limine; boot-menu snapshot rollback is not available |
| `limine` | x86 BIOS/UEFI bootloader; the CM5 boots via Raspberry Pi firmware and config.txt |
| `linux-firmware-marvell` | Microsoft Surface wifi firmware |
| `linux-firmware` | Microsoft Surface wifi firmware |
| `linux-headers` | replaced by our uConsole kernel headers |
| `linux-ptl-headers` | Intel Panther Lake kernel; x86-only |
| `linux-ptl` | Intel Panther Lake kernel; x86-only |
| `linux` | replaced by our uConsole kernel (stage 10) |
| `linux-t2` | Apple T2 MacBook kernel; x86-only |
| `linux-t2-headers` | Apple T2 MacBook kernel; x86-only |
| `macbook12-spi-driver-dkms` | Apple MacBook SPI keyboard |
| `nvidia-580xx-dkms` | no NVIDIA GPU on a CM5 |
| `nvidia-580xx-utils` | no NVIDIA GPU on a CM5 |
| `nvidia-dkms` | no NVIDIA GPU on a CM5 |
| `nvidia-open-dkms` | no NVIDIA GPU on a CM5 |
| `nvidia-utils` | no NVIDIA GPU on a CM5 |
| `qmk-hid` | Framework 16 keyboard firmware tool |
| `snapper` | Omarchy's snapshot story is limine + btrfs rollback entries; we ship ext4 (see docs/03-not-included.md) |
| `t2fanrd` | Apple T2 MacBook fan control |
| `thermald` | Intel thermal daemon; x86-only (the CM5 uses the RPi thermal driver) |
| `tuxedo-drivers-nocompatcheck-dkms` | TUXEDO laptop drivers |
| `vpl-gpu-rt` | Intel oneVPL runtime; x86-only |
| `vulkan-asahi` | Apple Silicon GPU driver; wrong ARM platform |
| `vulkan-intel` | no Intel GPU on a CM5 |
| `vulkan-radeon` | no AMD GPU on a CM5 |
| `yt6801-dkms` | Motorcomm YT6801 PCIe ethernet; not present on a uConsole |

## Packages built for aarch64 here

These do not exist in the Arch Linux ARM repositories, so stage 30 builds them.

| Package | Source |
|---|---|
| `aether` | omarchy-pkgs |
| `cliamp` | omarchy-pkgs |
| `herdr` | omarchy-pkgs |
| `hyprland-preview-share-picker` | omarchy-pkgs |
| `localsend` | omarchy-pkgs |
| `mise-bin` | omarchy-pkgs |
| `omacalc` | omarchy-pkgs |
| `omacut` | omarchy-pkgs |
| `omarchy-nvim` | omarchy-pkgs |
| `omawrite` | omarchy-pkgs |
| `tensaku` | omarchy-pkgs |
| `tobi-try` | omarchy-pkgs |
| `ttf-ia-writer` | omarchy-pkgs |
| `ttf-jetbrains-mono-nerd-basic` | omarchy-pkgs |
| `ttfx` | omarchy-pkgs |
| `tzupdate` | omarchy-pkgs |
| `ufw-docker` | omarchy-pkgs |
| `xdg-terminal-exec` | omarchy-pkgs |
| `yaru-icon-theme` | omarchy-pkgs |
| `yay` | omarchy-pkgs |
| `dotnet-runtime` | AUR |
| `nvim` | AUR |
| `obsidian` | AUR |
| `obs-studio` | AUR |
| `pinta` | AUR |
| `qemu-user-static-binfmt` | AUR |
| `yay-debug` | AUR |

## Build results

What actually happened the last time stage 30 ran on this machine.

| Package | Status | Detail |
|---|---|---|
| `tzupdate` | built | tzupdate-3.1.0-1-aarch64.pkg.tar.zst  |
| `ufw-docker` | built | ufw-docker-251123-1-any.pkg.tar.zst  |
| `omacalc` | built | omacalc-0.2.2-1-aarch64.pkg.tar.zst  |
| `omacut` | built | omacut-0.4.0-1-aarch64.pkg.tar.zst  |
| `omawrite` | built | omawrite-0.5.0-1-aarch64.pkg.tar.zst  |
| `ttfx` | built | ttfx-0.3.2-1-aarch64.pkg.tar.zst  |
| `omarchy-nvim` | built | omarchy-nvim-2026.8.13-1-any.pkg.tar.zst  |

A package that failed here is simply absent from the image; the rest
of Omarchy is unaffected.
