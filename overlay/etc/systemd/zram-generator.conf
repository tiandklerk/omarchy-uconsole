# Omarchy enables zram by default. On the uConsole the win is avoiding writes
# to the SD card, not memory pressure - a CM5 Lite 16GB rarely swaps.
[zram0]
zram-size = min(ram / 4, 4096)
compression-algorithm = zstd
